# Test Coverage Map

## Test Coverage

| Feature Name | Test File Path | Total Tests | Status |
| --- | --- | --- | --- |
| Shared | `src/hooks/useResizablePanels.test.ts` | 12 | Not recorded |
| Shared Components | `src/components/Modal/ConfirmationModal.test.tsx` | 2 | Not recorded |
| Shared Components | `src/components/Modal/TextInputModal.test.tsx` | 3 | Not recorded |
| Shared Components | `src/components/layout/AppLayout.test.tsx` | 10 | Not recorded |
| Shared Components | `src/components/notifications/toastLifecycle.test.ts` | 4 | Not recorded |
| Shared Components | `src/components/resize-divider/ResizeDivider.test.tsx` | 6 | Not recorded |
| Shared Components | `src/components/search-bar/SearchBar.test.tsx` | 6 | Not recorded |
| Shared Components | `src/components/tabs/Tabs.test.tsx` | 2 | Not recorded |
| Shared Stores | `src/stores/canvas-store.test.ts` | 1 | Not recorded |
| Shared Stores | `src/stores/workspace-store.test.ts` | 3 | Not recorded |
| Shared Stores | `src/stores/workspace-update-sync.test.ts` | 3 | Not recorded |
| auth-profile | `src/features/auth-profile/components/ProfileDropdown.test.tsx` | 1 | Not recorded |
| auth-profile | `src/features/auth-profile/components/ProfileListItem.test.tsx` | 1 | Not recorded |
| auth-profile | `src/features/auth-profile/components/ProfileManagementModal.test.tsx` | 11 | Not recorded |
| auth-profile | `src/features/auth-profile/components/RepositoryProfileAssignmentControl.test.tsx` | 3 | Not recorded |
| auth-profile | `src/features/auth-profile/stores/profileStore.test.ts` | 3 | Not recorded |
| auto-update | `src/features/auto-update/AutoUpdatePanel.test.tsx` | 3 | Not recorded |
| auto-update | `src/features/auto-update/coordinator.test.ts` | 9 | Not recorded |
| branch-map | `src/features/branch-map/components/MapToolbar.test.tsx` | 4 | Not recorded |
| branch-map | `src/features/branch-map/components/ViewActionsDropdown.test.tsx` | 12 | Not recorded |
| branch-map | `src/features/branch-map/components/ViewSelectorTabs.test.tsx` | 6 | Not recorded |
| branch-map | `src/features/branch-map/viewportSync.test.ts` | 3 | Not recorded |
| canvas-views | `src/features/canvas-views/components/RepositoryScopeSelector.test.tsx` | 8 | Not recorded |
| canvas-views | `src/features/canvas-views/components/ViewManagerSidebar.test.tsx` | 3 | Not recorded |
| canvas-views | `src/features/canvas-views/components/scopeSelection.test.ts` | 7 | Not recorded |
| dashboard | `src/features/dashboard/components/DashboardMain.test.tsx` | 9 | Not recorded |
| dashboard | `src/features/dashboard/components/RepositoryCard.test.tsx` | 13 | Not recorded |
| dashboard | `src/features/dashboard/components/RepositoryCard/RepoCardHeader.test.tsx` | 1 | Not recorded |
| dashboard | `src/features/dashboard/components/RepositoryCard/RepoCardTags.test.tsx` | 4 | Not recorded |
| dashboard | `src/features/dashboard/components/RepositoryCard/RepoGroupMenu.test.tsx` | 3 | Not recorded |
| dashboard | `src/features/dashboard/components/RepositoryCard/RepoTagSelectionMenu.test.tsx` | 3 | Not recorded |
| dashboard | `src/features/dashboard/components/RepositoryCard/RepoThemeModal.test.tsx` | 2 | Not recorded |
| dashboard | `src/features/dashboard/components/RepositoryCard/menuPosition.test.ts` | 2 | Not recorded |
| dashboard | `src/features/dashboard/components/common/FilterDropdown.test.tsx` | 5 | Not recorded |
| github-auth | `src/features/github-auth/hooks/useGithubRepositories.test.tsx` | 3 | Not recorded |
| management | `src/features/management/components/SettingsManagementModal.test.tsx` | 8 | Not recorded |
| onboarding | `src/features/onboarding/components/OnboardingPresentation.test.tsx` | 6 | Not recorded |
| onboarding | `src/features/onboarding/hooks/useOnboarding.test.tsx` | 2 | Not recorded |
| repository | `src/features/repository/components/AddLocalRepositoryModal.test.tsx` | 2 | Not recorded |
| repository | `src/features/repository/components/BulkImportLocalRepositoryModal.test.tsx` | 2 | Not recorded |
| repository | `src/features/repository/components/CloneRemoteRepositoryModal.test.tsx` | 19 | Passed |
| repository | `src/features/repository/components/RepositoryDropdown.test.tsx` | 2 | Not recorded |
| repository | `src/features/repository/stores/import-store.test.ts` | 3 | Not recorded |
| repository-detail | `src/features/repository-detail/components/RepositoryDetail.test.tsx` | 8 | Not recorded |
| repository-detail | `src/features/repository-detail/components/RepositoryDetailActionsMenu.test.tsx` | 12 | Not recorded |
| repository-detail | `src/features/repository-detail/components/RepositoryDetailBody.test.tsx` | 6 | Not recorded |
| repository-detail | `src/features/repository-detail/components/RepositoryDetailChangesTab.test.tsx` | 9 | Not recorded |
| repository-detail | `src/features/repository-detail/components/RepositoryDetailCommitsTab.test.tsx` | 13 | Not recorded |
| repository-detail | `src/features/repository-detail/components/RepositoryDetailHeader.test.tsx` | 1 | Not recorded |
| repository-detail | `src/features/repository-detail/hooks/useCommitChangedFiles.test.ts` | 5 | Not recorded |
| repository-detail | `src/features/repository-detail/hooks/useCommitFileDiff.test.ts` | 5 | Not recorded |
| repository-update-diagnostics | `src/features/repository-update-diagnostics/components/RepositoryUpdateDiagnosticsModal.test.tsx` | 3 | Not recorded |

## Test Label Matrix

| Feature Name | Test File Path | Describe and Test Labels |
| --- | --- | --- |
| Shared | `src/hooks/useResizablePanels.test.ts` | `caps drags at maxPx-derived effective maxima when provided`<br>`clamps drag ratios to ratio bounds`<br>`converts pixel floors into effective ratios against the live rect`<br>`enforces the pixel floor while dragging at a wide container size`<br>`initializes each divider ratio from its config default`<br>`invokes the latest onRatiosCommit callback without restarting ratio state`<br>`keeps per-divider ratios independent when one divider moves`<br>`locks global cursor and user-select during drag and restores on release`<br>`nudges ratios via resizeBy within effective clamps`<br>`reports committed ratios on mount, skips mid-drag, and reports after release`<br>`restores global styles when unmounting mid-drag`<br>`seeds initial ratios from options and falls back to config defaults`<br>`useResizablePanels` |
| Shared Components | `src/components/Modal/ConfirmationModal.test.tsx` | `ConfirmationModal`<br>`does not dismiss when the interaction starts inside the dialog and ends on the backdrop`<br>`uses the danger button styling for destructive confirmations` |
| Shared Components | `src/components/Modal/TextInputModal.test.tsx` | `TextInputModal`<br>`disables the reset button while busy`<br>`does not render a reset button when no reset action is provided`<br>`renders the reset button and invokes onReset when clicked` |
| Shared Components | `src/components/layout/AppLayout.test.tsx` | `AppLayout`<br>`applies shared title bar button classes and active state styling to the header actions`<br>`closes the notification dropdown when the bell button is clicked while it is open`<br>`closes the notification dropdown when the repository menu is opened`<br>`closes the other dropdowns when the profile dropdown is opened`<br>`closes the profile dropdown when its trigger is clicked again`<br>`creates a view from the global repository menu before navigating to the branch map`<br>`delegates onboarding add-local requests to the existing modal owner and resumes on close`<br>`does not navigate when the global create-view action fails`<br>`opens profile management for the active profile without selecting another profile`<br>`places the repository diagnostics action beside the notification action` |
| Shared Components | `src/components/notifications/toastLifecycle.test.ts` | `ignores repeated exit completion for an already removed toast`<br>`keeps only three toasts visible and queues later notifications in FIFO order`<br>`pauses with the exact remaining duration and resumes from that point`<br>`promotes the oldest queued toast after an exiting toast finishes`<br>`toastLifecycleReducer` |
| Shared Components | `src/components/resize-divider/ResizeDivider.test.tsx` | `ResizeDivider`<br>`clamps the reported aria value to the effective bounds`<br>`exposes separator semantics and aria values derived from ratio and bounds`<br>`nudges with arrow keys within clamps and ignores other keys`<br>`prevents native drag behavior`<br>`starts resizing on mousedown with the default prevented`<br>`supports an additional layout className` |
| Shared Components | `src/components/search-bar/SearchBar.test.tsx` | `SearchBar`<br>`can suppress its shared focus ring when a parent wrapper provides one`<br>`can suppress its shell border when a parent wrapper provides the outline`<br>`renders a visible shell around the input`<br>`renders helper text when the field is empty`<br>`shows a clear button when it has text and clears the input`<br>`supports an opt-in compact size without changing the default shell contract` |
| Shared Components | `src/components/tabs/Tabs.test.tsx` | `Tabs`<br>`moves focus and changes selection with arrow keys`<br>`renders accessible active state and changes tabs` |
| Shared Stores | `src/stores/canvas-store.test.ts` | `canvas store hydration`<br>`hydrates viewport, persisted nodes, branch sync data, topology, and manual edges` |
| Shared Stores | `src/stores/workspace-store.test.ts` | `clears stale missing status for repositories that still exist`<br>`does not emit a new state when a repository is already marked resolved`<br>`marks repositories missing for matching absolute paths`<br>`workspace store missing repo handling` |
| Shared Stores | `src/stores/workspace-update-sync.test.ts` | `refreshes repository data without hydrating canvas nodes when no branch map is registered`<br>`rejects malformed and oversized invalidation payloads`<br>`shares one listener and ignores duplicate repository revisions`<br>`workspace update synchronization` |
| auth-profile | `src/features/auth-profile/components/ProfileDropdown.test.tsx` | `ProfileDropdown`<br>`keeps the overlay from blocking window interactions while preserving outside-click closing` |
| auth-profile | `src/features/auth-profile/components/ProfileListItem.test.tsx` | `ProfileListItem`<br>`selects the profile and toggles favorite state from separate controls` |
| auth-profile | `src/features/auth-profile/components/ProfileManagementModal.test.tsx` | `ProfileManagementModal`<br>`asks for confirmation before deleting a profile from the profile list`<br>`asks for confirmation before deleting the active profile from the footer action`<br>`bounds the expanded profile workspace when many profiles are present`<br>`clears the current selection and starts a blank draft when switching to create mode`<br>`closes the modal only when the backdrop press is released on the backdrop`<br>`does not close the modal when the backdrop press is released outside the overlay`<br>`keeps the modal open after saving an existing profile`<br>`keeps the modal open and switches to editing after creating a new profile`<br>`keeps the selected row highlighted when hovered`<br>`selects the first existing profile after deleting the unsaved draft`<br>`warns that a fresh default profile will be recreated after deleting the final profile` |
| auth-profile | `src/features/auth-profile/components/RepositoryProfileAssignmentControl.test.tsx` | `RepositoryProfileAssignmentControl`<br>`assigns a profile and refreshes the resolved state`<br>`displays the resolved profile and resolution source without an assignment`<br>`shows stale assignments and offers a destructive clear confirmation` |
| auth-profile | `src/features/auth-profile/stores/profileStore.test.ts` | `forwards full OAuth classification when updating a profile`<br>`includes the api_base_url when updating a profile so saved edits persist`<br>`profile store token health`<br>`refreshes token health when a full OAuth profile becomes active` |
| auto-update | `src/features/auto-update/AutoUpdatePanel.test.tsx` | `AutoUpdatePanel`<br>`disables check actions while checking and reports preview mode honestly`<br>`exposes status and progressbar semantics for an available update`<br>`requires an explicit restart confirmation after installation` |
| auto-update | `src/features/auto-update/coordinator.test.ts` | `UpdateCoordinator`<br>`checks metadata without downloading installer bytes`<br>`guards duplicate downloads and duplicate checks`<br>`invalidates a deferred update object before a later manual check`<br>`keeps progress monotonic when native events arrive out of order`<br>`reports unsupported preview runtime honestly`<br>`requires consent for download and install, and uses explicit no-restart installation`<br>`requires explicit restart confirmation`<br>`returns retryable offline failures and recovers on retry`<br>`suppresses only the deferred version during a startup check` |
| branch-map | `src/features/branch-map/components/MapToolbar.test.tsx` | `MapToolbar`<br>`opens the tag filters popover and toggles tags through the toolbar`<br>`remembers the tag filters popover state for the current session`<br>`renders nothing when hidden without breaking hook order`<br>`routes reset and fit actions through parent callbacks` |
| branch-map | `src/features/branch-map/components/ViewActionsDropdown.test.tsx` | `ViewActionsDropdown`<br>`blocks blank rename input and cancels without renaming`<br>`closes when an outside interaction stops propagation`<br>`closes when clicking outside the menu`<br>`disables restore when no saved location snapshot exists`<br>`keeps the dropdown open when the rename modal is dismissed from its backdrop`<br>`keeps the dropdown open when the rename modal is dismissed with Escape`<br>`keeps the rename modal open when the rename operation rejects`<br>`opens a confirmation modal before deleting the active view`<br>`opens a confirmation modal before restoring saved card locations`<br>`opens the shared rename modal and renames the active view on submit`<br>`renders Order and State Options as collapsible submenu groups with the delete action styled as dangerous`<br>`shows a compact quick action row with tooltips for the most common actions` |
| branch-map | `src/features/branch-map/components/ViewSelectorTabs.test.tsx` | `ViewSelectorTabs`<br>`dismisses overflow when an outside canvas surface stops propagation`<br>`opens the overflow menu, selects a view, and closes on selection`<br>`renders visible views as accessible tabs and selects a view`<br>`supports roving tab focus and arrow, Home, and End navigation`<br>`updates the visible tab count when crossing responsive breakpoints`<br>`wraps long view names in a constrained label with the full tooltip name` |
| branch-map | `src/features/branch-map/viewportSync.test.ts` | `applies the stored viewport when switching to a different view`<br>`does not reapply the stored viewport during an active view save`<br>`reapplies when the active view changes even if the viewport values match the last applied state`<br>`shouldApplyStoredViewport` |
| canvas-views | `src/features/canvas-views/components/RepositoryScopeSelector.test.tsx` | `RepositoryScopeSelector`<br>`enables scrolling only when requested by the scope tab`<br>`filters by display name and alias without matching branch names`<br>`filters by group and tag without changing selection callbacks`<br>`keeps bulk actions global while repositories are filtered`<br>`keeps scope controls interactive while a repository save is pending`<br>`normalizes structured and serialized tags defensively`<br>`restores all repositories when the search is cleared`<br>`shows a distinct message when the search has no matches` |
| canvas-views | `src/features/canvas-views/components/ViewManagerSidebar.test.tsx` | `ViewManagerSidebar`<br>`keeps view actions independent from row selection`<br>`selects a view from the row body and keyboard activation`<br>`shows the selected view name in the sidebar context` |
| canvas-views | `src/features/canvas-views/components/scopeSelection.test.ts` | `removes duplicate and stale branch names`<br>`reports unchecked when the repository path is hidden`<br>`returns all available branches when a repository is selected`<br>`scope selection helpers`<br>`treats a repository with every branch selected as checked`<br>`treats a repository with no branches selected as unchecked`<br>`treats a repository with some branches selected as mixed`<br>`uses repository visibility for repositories without branches` |
| dashboard | `src/features/dashboard/components/DashboardMain.test.tsx` | `DashboardMain`<br>`clears the search input when Escape is pressed`<br>`defaults the sort dropdown to Alphabetical`<br>`does not reverify when repository status changes`<br>`lists Alphabetical before Last Accessed in the sort dropdown`<br>`renders repositories alphabetically by default`<br>`shows a clear button when the search input has text and clears it on click`<br>`shows repository card skeletons before the initial workspace hydration completes`<br>`shows repository cards as soon as the workspace rows are hydrated`<br>`shows the empty state after hydration when no repositories match` |
| dashboard | `src/features/dashboard/components/RepositoryCard.test.tsx` | `RepositoryCard`<br>`checks out a branch and refreshes repository state afterwards`<br>`closes the details view when navigation changes`<br>`does not open the details view when double-clicking the interactive icon area`<br>`does not prevent selection on interactive card descendants`<br>`does not render clone again for Local Only repositories in the missing state`<br>`hides the push-needed indicator when the branch has no upstream`<br>`hides the push-needed indicator when there are no local commits to push`<br>`opens the details view when double-clicking empty card space`<br>`prevents only the second primary mouse-down on empty card space`<br>`reattaches the existing tracked repository when locating a missing repository`<br>`renders the pinned card state from hydrated repository data`<br>`resets the locate button label when the repository changes`<br>`shows the push-needed indicator when the branch is ahead of its upstream` |
| dashboard | `src/features/dashboard/components/RepositoryCard/RepoCardHeader.test.tsx` | `RepoCardHeader`<br>`renders a human-friendly owned label` |
| dashboard | `src/features/dashboard/components/RepositoryCard/RepoCardTags.test.tsx` | `RepoCardTags`<br>`renders an overflow badge when the tag list is too long to fit inline`<br>`renders tags in alphabetical order inside the overflow popover`<br>`renders the tag remove control as an icon button`<br>`uses a single visible tag on narrow cards to preserve space for the group name` |
| dashboard | `src/features/dashboard/components/RepositoryCard/RepoGroupMenu.test.tsx` | `RepoGroupMenu`<br>`keeps the color dot from shrinking when a group name is long`<br>`shows a popover with the full group name when hovering a truncated badge label`<br>`uses a comfortable line-height for the badge label so the text is not clipped` |
| dashboard | `src/features/dashboard/components/RepositoryCard/RepoTagSelectionMenu.test.tsx` | `TagSelectionModal`<br>`applies tag changes immediately when a tag is toggled`<br>`filters tags and clears the text search independently from tag assignment`<br>`uses a done action to close the modal` |
| dashboard | `src/features/dashboard/components/RepositoryCard/RepoThemeModal.test.tsx` | `RepoThemeModal`<br>`keeps submit-mode changes local until Apply to selected is pressed`<br>`submits reset defaults as null values in submit mode` |
| dashboard | `src/features/dashboard/components/RepositoryCard/menuPosition.test.ts` | `getViewportSafeMenuPosition`<br>`opens above the trigger when the bottom space is insufficient`<br>`opens below the trigger when there is enough room` |
| dashboard | `src/features/dashboard/components/common/FilterDropdown.test.tsx` | `FilterDropdown`<br>`exposes accessible state for the trigger`<br>`hides the placeholder/reset option when requested`<br>`renders static options and selects one value`<br>`shows loading and error states for async options`<br>`supports multi-select behavior` |
| github-auth | `src/features/github-auth/hooks/useGithubRepositories.test.tsx` | `does not invoke the backend without a profile id`<br>`loads repositories through the profile-id Tauri command`<br>`loads the next page through the same backend boundary`<br>`useGithubRepositories` |
| management | `src/features/management/components/SettingsManagementModal.test.tsx` | `SettingsManagementModal`<br>`clears a pending tag confirmation when the modal is reopened`<br>`closes with Escape and restores the previous body overflow value`<br>`confirms tag deletion once and removes the confirmation`<br>`exposes accessible tabs, panels, and color controls`<br>`keeps the Tags tab selected when the tag list refreshes after creation`<br>`keeps the management modal open when Escape closes a child confirmation`<br>`renders when open without crashing on backdrop handlers`<br>`restores and purges archived repositories through the archive tab` |
| onboarding | `src/features/onboarding/components/OnboardingPresentation.test.tsx` | `OnboardingPresentation`<br>`emits typed requests for existing profile and repository owners`<br>`keeps navigation actions at the step boundaries and delegates movement`<br>`locks body overflow, focuses the dialog, and restores focus on close`<br>`marks the active step for assistive technology`<br>`renders each progressive step with accessible progress`<br>`uses safe Escape and pointer-aware backdrop dismissal without persistence in test mode` |
| onboarding | `src/features/onboarding/hooks/useOnboarding.test.tsx` | `does not persist skip when the onboarding presentation is opened in test mode`<br>`opens startup onboarding when the persisted state is not started`<br>`useOnboarding persistence semantics` |
| repository | `src/features/repository/components/AddLocalRepositoryModal.test.tsx` | `AddLocalRepositoryModal notifications`<br>`keeps repository-add failures in the notification inbox`<br>`uses a toast-only notification when a repository is added` |
| repository | `src/features/repository/components/BulkImportLocalRepositoryModal.test.tsx` | `BulkImportLocalRepositoryModal notifications`<br>`keeps bulk-import failures in the notification inbox`<br>`uses a toast-only notification when repositories are imported` |
| repository | `src/features/repository/components/CloneRemoteRepositoryModal.test.tsx` | `CloneRemoteRepositoryModal`<br>`applies fixed list height to basic tab only`<br>`keeps URL clone failures in the notification inbox`<br>`keeps selected-repository clone failures in the notification inbox`<br>`keeps the basic repository list at a consistent height when there are no results`<br>`passes the URL subfolder preference to the clone command`<br>`refresh button forces basic reload when clicked`<br>`renders grouped repositories when not loading`<br>`renders last updated status in the basic tab`<br>`renders loading status before repository groups when loading with existing items`<br>`shows a clear button for repository search and clears the input`<br>`shows a descriptive message when invoke rejects with a plain string error`<br>`shows a descriptive message when repository loading fails`<br>`shows a descriptive message when the selected profile is not a full OAuth profile`<br>`shows enterprise last updated status and force refreshes while loading`<br>`shows enterprise locked state and opens profile management when api base URL is missing`<br>`shows loading state and suppresses empty state during initial loading`<br>`uses a toast-only notification when cloning a selected repository succeeds`<br>`uses a toast-only notification when cloning from a URL succeeds`<br>`validates URL clone input before invoking clone command` |
| repository | `src/features/repository/components/RepositoryDropdown.test.tsx` | `RepositoryDropdown`<br>`enables clone action when remote clone prerequisites are met`<br>`keeps clone disabled when remote clone prerequisites are not met` |
| repository | `src/features/repository/stores/import-store.test.ts` | `cancels pending items after the current invocation completes`<br>`continues after failures and reports aggregate counts`<br>`retries only failed items from the completed job`<br>`useImportStore` |
| repository-detail | `src/features/repository-detail/components/RepositoryDetail.test.tsx` | `RepositoryDetail`<br>`does not reload commits when workspace hydration replaces the repository object`<br>`injects the repository view overlay styles when opened`<br>`keeps custom panel widths across tab switches and resets them when the dialog closes`<br>`loads branch commits and updates the changed-files column when a commit is selected`<br>`promotes only the open repository detail session and cleans it up on close`<br>`reloads the commits list when a git operation completes from the actions menu`<br>`renders the commits and changes tabs and opens the compact repository summary popover`<br>`renders the dialog in a portal outside the repository card container` |
| repository-detail | `src/features/repository-detail/components/RepositoryDetailActionsMenu.test.tsx` | `RepositoryDetailActionsMenu`<br>`archives through the confirmation modal and closes the detail view`<br>`clears the alias through the modal reset action`<br>`completes a push and reports the backend message`<br>`hides the clear alias action when the repository has no alias`<br>`labels the push action as publication when the branch has no upstream`<br>`notifies history listeners after a git operation completes`<br>`opens the grouped action menu and hides remote actions for local-only repositories`<br>`propagates backend push errors to the toast unchanged`<br>`renames the alias through the text input modal`<br>`requires explicit confirmation before replacing an existing remote branch`<br>`runs fetch through the backend and syncs git status afterwards`<br>`shows pull and push for repositories with a remote` |
| repository-detail | `src/features/repository-detail/components/RepositoryDetailBody.test.tsx` | `RepositoryDetailBody`<br>`collapses and expands a changes group without affecting its stage action`<br>`keeps the changes list as the vertical scroll container`<br>`loads the selected file diff and renders both preview modes`<br>`prevents text selection while resizing the changes layout`<br>`renders grouped changes and commit composer for the changes tab`<br>`renders repository summary details and commit history state` |
| repository-detail | `src/features/repository-detail/components/RepositoryDetailChangesTab.test.tsx` | `RepositoryDetailChangesTab`<br>`commits nudged ratios to the persistence handler after keyboard resize`<br>`creates a commit using a trimmed title and optional message`<br>`keeps the changes list as the vertical scroll container`<br>`loads the selected file diff and renders both preview modes`<br>`prevents text selection while resizing the changes layout`<br>`renders grouped changes, commits, and preserves the stage action when a group is collapsed`<br>`renders the change list before a slow selected-file diff resolves`<br>`restores persisted panel ratios on mount and falls back to defaults without them`<br>`stages and unstages a selected file with refreshed snapshots` |
| repository-detail | `src/features/repository-detail/components/RepositoryDetailCommitsTab.test.tsx` | `RepositoryDetailCommitsTab`<br>`adjusts panel flex-basis within clamp bounds when dividers are dragged`<br>`auto-selects the first file, loads its diff, and flips view modes`<br>`commits the final ratio to the persistence handler after a drag completes`<br>`expands the commit summary strip with metadata and resets expansion per commit`<br>`fetches changed files when a commit is selected and renders rows with badges`<br>`loads the diff when the user picks another file`<br>`nudges the files ratio with arrow keys on the focused divider`<br>`renders loading, empty, and loaded history states`<br>`renders relative commit dates with full-date tooltips`<br>`restores persisted panel ratios on mount and reports them upward`<br>`shows the empty-commit state without collapsing the layout`<br>`shows the truncation notice above a partial diff`<br>`surfaces binary files with the established empty-state message` |
| repository-detail | `src/features/repository-detail/components/RepositoryDetailHeader.test.tsx` | `RepositoryDetailHeader`<br>`renders repository title and summary details` |
| repository-detail | `src/features/repository-detail/hooks/useCommitChangedFiles.test.ts` | `clears state without fetching when inputs are missing`<br>`fetches changed files for a commit and exposes them`<br>`supports clearing files on demand via clearFiles`<br>`suppresses stale responses after the commit changes`<br>`surfaces errors from the command`<br>`useCommitChangedFiles` |
| repository-detail | `src/features/repository-detail/hooks/useCommitFileDiff.test.ts` | `does not fetch when any input is missing`<br>`fetches the commit file diff and exposes it`<br>`supports clearing the cached diff via clearFileDiff`<br>`suppresses stale responses when the selection changes quickly`<br>`surfaces errors from the command`<br>`useCommitFileDiff` |
| repository-update-diagnostics | `src/features/repository-update-diagnostics/components/RepositoryUpdateDiagnosticsModal.test.tsx` | `RepositoryUpdateDiagnosticsModal`<br>`clears search on the first Escape and closes on the next Escape`<br>`dismisses from a backdrop press but not when a press starts inside the dialog`<br>`filters repositories and clears the search` |

## Coverage Gaps

Feature folders without a corresponding test file:

- `src/features/icon/`
- `src/features/repository-open/`

